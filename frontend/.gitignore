node_modules
dist
dist-ssr
*.local
.DS_Store

# Local env files — Tomi: put OPENAI keys in .env.local (server-side only, never commit)
.env
.env.*
!.env.example

# Editor
.vscode/*
!.vscode/extensions.json
.idea
